import { Badge } from "@/components/ui/badge";
import { ArrowUpRight, CalendarClock, CheckCircle2, Clock3, Users } from "lucide-react";

const stats = [{ label: "Total employees", value: "--", icon: Users, detail: "Connect Supabase to load" }, { label: "Present today", value: "--", icon: CheckCircle2, detail: "Attendance data pending" }, { label: "Pending approvals", value: "--", icon: Clock3, detail: "Leave data pending" }, { label: "Upcoming holidays", value: "--", icon: CalendarClock, detail: "Holiday data pending" }];

export default function DashboardPage() {
  return <div className="mx-auto max-w-[1400px]"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-[var(--teal)]">Overview</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-[var(--navy)]">People operations</h1><p className="mt-2 text-sm text-slate-500">Your workspace at a glance. Live metrics will appear as modules are connected.</p></div><Badge>Foundation ready</Badge></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ label, value, icon: Icon, detail }) => <div key={label} className="border bg-white p-5 shadow-[0_2px_10px_rgba(16,42,67,0.03)]"><div className="flex items-start justify-between"><p className="text-sm font-semibold text-slate-600">{label}</p><Icon size={19} className="text-[var(--teal)]" /></div><p className="mt-5 text-3xl font-bold text-[var(--navy)]">{value}</p><p className="mt-1 text-xs text-slate-400">{detail}</p></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.4fr_1fr]"><div className="border bg-white p-6"><div className="flex items-center justify-between"><div><h2 className="font-bold text-[var(--navy)]">Workspace activity</h2><p className="mt-1 text-xs text-slate-400">A snapshot of recent HR operations</p></div><ArrowUpRight size={18} className="text-slate-400" /></div><div className="flex min-h-64 flex-col items-center justify-center text-center"><div className="mb-3 h-px w-20 bg-[#bfe8e6]" /><p className="text-sm font-semibold text-slate-600">No activity to display yet</p><p className="mt-1 max-w-xs text-xs leading-5 text-slate-400">Activity will populate when your team begins using the HRMS modules.</p></div></div><div className="border bg-[var(--navy)] p-6 text-white"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#76d1c9]">Next step</p><h2 className="mt-3 text-xl font-bold">Connect your workspace</h2><p className="mt-2 text-sm leading-6 text-white/60">Add Supabase credentials to enable secure data services when Phase 2 begins.</p><div className="mt-7 flex items-center gap-2 text-xs font-semibold text-[#bfe8e6]"><span className="h-2 w-2 rounded-full bg-[#76d1c9]" /> Environment ready for configuration</div></div></div></div>;
}