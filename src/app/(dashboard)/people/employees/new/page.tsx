import { EmployeeForm } from "@/features/people/components/employee-form";
import { listDepartments, listDesignations, listManagers, listOrganizations } from "@/features/people/services";
export default async function NewEmployeePage() { const [{ data: organizations = [] }, { data: departments = [] }, { data: designations = [] }, { data: managers = [] }] = await Promise.all([listOrganizations(), listDepartments(), listDesignations(), listManagers()]); return <section className="mx-auto max-w-4xl"><p className="text-sm font-semibold text-[var(--teal)]">People / Employees</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-[var(--navy)]">Add employee</h1><p className="mt-2 text-sm text-slate-500">Create a secure employee record for your organization.</p><div className="mt-8 border bg-white p-5 sm:p-8"><EmployeeForm organizations={organizations} departments={departments} designations={designations} managers={managers} /></div></section>; }